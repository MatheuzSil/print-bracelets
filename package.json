{
  "name": "teste-impressora",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node run.js",
    "setup": "node src/setup.js",
    "direct": "node src/print-bracelets.js",
    "docker:build": "docker build -t print-bracelets .",
    "docker:run": "docker run -it --rm --name print-bracelets-system print-bracelets",
    "docker:start": "npm run docker:build && npm run docker:run",
    "docker:tag": "docker tag print-bracelets matheuzsilva/print-bracelets:latest",
    "docker:push": "docker push matheuzsilva/print-bracelets:latest",
    "docker:deploy": "npm run docker:build && npm run docker:tag && npm run docker:push",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "amqplib": "^0.10.8"
  }
}
